server:
  port: 8080
  mode: debug

database:
  host: localhost
  port: 3306
  user: root
  password: rootpassword
  name: opc_aicom
  max_idle_conns: 10
  max_open_conns: 100
  conn_max_lifetime: 3600

jwt:
  secret: your-secret-key-change-in-production
  expire_hours: 24
  cookie:
    name: auth_token
    path: /
    domain: ""
    secure: false      # 生产环境改为 true (HTTPS)
    max_age: 86400     # 24小时，单位秒

matrix:
  homeserver_url: http://8.217.143.228:8008
  server_name: 8.217.143.228
  shared_secret: nexus_matrix_secret_xK9mP2vL7qR4wY6j
  admin_user: bite
  admin_password: 123bite123321
  workers:
    - worker-001
    - worker-002
  official_rooms:
    - name: "OPC之家"
      topic: "OPC社区官方讨论区，欢迎所有成员"
      alias: "opc-home"
    - name: "AI技术交流"
      topic: "讨论AI技术、分享学习资源"
      alias: "ai-discussion"
    - name: "项目展示"
      topic: "展示你的项目，获取反馈"
      alias: "project-showcase"
llm:
  default_provider: openai
  openai:
    api_key: ""       # Set via OPENAI_API_KEY env var
    base_url: ""
  anthropic:
    api_key: ""       # Set via ANTHROPIC_API_KEY env var
    base_url: ""

# Workspace storage configuration
workspace:
  type: local           # "local" or "oss"
  local_dir: uploads/workspace
  oss:
    endpoint: ""           # e.g. "oss-cn-hangzhou.aliyuncs.com"
    access_key_id: ""
    access_key_secret: ""
    bucket_name: ""
    region: ""
    prefix: "openc-aicom/workspace/"
