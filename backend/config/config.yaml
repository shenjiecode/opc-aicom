server:
  port: 8080
  mode: debug

database:
  host: localhost
  port: 3306
  user: root
  password: rootpassword
  name: opc_aicom
  max_idle_conns: 10
  max_open_conns: 100
  conn_max_lifetime: 3600

jwt:
  secret: your-secret-key-change-in-production
  expire_hours: 24
